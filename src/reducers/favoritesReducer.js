export const initialState = {
    favorites: [{id:1, title: "testing"}, {id:2, title: "testing2"}],
    displayFavorites: true
}

const favoriteReducer = (state = initialState, action) => {
    switch(action.type) {
        
        default: 
            return (state) 
    }


}

export default favoriteReducer